export class SomeClass {
    private x: number = 10;

    public printX(): void {
        console.log(this.x);
    }

    public calculateSum(a: number, b: number): number {
        return a + b;
    }
}
